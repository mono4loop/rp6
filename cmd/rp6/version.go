package main

// appID is the application's unique ID. It must match the flatpak app-id
// (flatpak/io.github.mono4loop.rp6.yml) so Preferences and desktop integration
// resolve to the same identity.
const appID = "io.github.mono4loop.rp6"

// version is the RP6 release version, shown in the info dialog.
const version = "v1.0.0-alpha.7"
